platform :ios, '10.0'

inhibit_all_warnings!

def shared_pods
	pod 'LHSCategoryCollection', '~> 0.0'
	pod 'SwiftSpinner'
	pod 'Alamofire', '~> 4.5'
	use_frameworks!
end

target 'UserPost' do
	shared_pods
end
